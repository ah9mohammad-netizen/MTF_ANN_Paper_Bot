# Async / HTTP
aiohttp>=3.9.0
python-dotenv>=1.0.0

# Database
SQLAlchemy>=2.0.23
aiosqlite>=0.19.0

# Exchange market data (XAU-USDT / PAXG)
ccxt>=4.2.0

# Optional typed config helpers
pydantic>=2.5.0

# Telegram libs available if you extend the UI later
# (core bot uses stdlib urllib — zero hard dependency at runtime)
aiogram>=3.3.0
